services:
  db:
    image: postgres:14
    environment:
      POSTGRES_DB: edu_modules
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: azerty1998
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      - DB_HOST=db
volumes:
  postgres_data: